package 数据结构.逻辑结构.线性结构.线性表.应用.一元多项式的运算;

/*
    Pa(x) = 10 + 5x - 4x^2 + 3x^3 + 2x^4
    Pb(x) = -3 + 8x + 4x^2 - 5x^4 + 7x^5 - 2x^6
    实现两个多项式的加减运算,用稀疏多项式来表达（即实现稀疏多项式的运算）
    ——>La = ((10,0),(5.1),(-4,2),(3.3),(2.4))
       Lb = ((-3, 0),(8,1),(4,2),(-5,4),(7,5),(2.6))
    1、用稀疏数组实现
    2、用链表实现
 */
public class Test02 {
}
